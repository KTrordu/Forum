@model IEnumerable<Forum.UI.ViewModels.TopicViewModel>

@{
    ViewData["Title"] = "Index";
}
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Topic List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Topic" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Topic
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Topic Name
            </th>
            <th>
                Created At
            </th>
            <th>
                
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="40%">
                    <a asp-controller="Post" asp-action="Index" asp-route-id="@obj.Id" style="text-decoration:none; color:navy">
                        @obj.TopicName
                    </a>
                </td>
                <td width="40%">
                    @obj.CreatedAt.Day.@obj.CreatedAt.Month.@obj.CreatedAt.Year @obj.CreatedAt.DayOfWeek
                </td>
                <td width="10%">
                    <div>
                        <a asp-controller="Topic" asp-action="Edit" asp-route-id="@obj.Id" style="text-decoration:none; color:navy">
                            <i class="bi bi-pencil-square" style="color:orange"></i> Edit</a>
                    </div>
                </td>
                <td width="10%">
                    <div>
                        <a asp-controller="Topic" asp-action="Delete" asp-route-id="@obj.Id" style="text-decoration:none; color:darkred">
                            <i class="bi bi-x-circle" style="color:darkred"></i> Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>